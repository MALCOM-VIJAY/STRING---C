#include <stdio.h>
#include <math.h>
char *copy(char b[],char a[]){
    int i=0;
    for( i=0;a[i]!='\0';i++){
        b[i]=a[i];
    }
    b[i]='\0';
    return b;
}
int main()
{
   char str1[]="hello";
   char str2[100];
   printf("%s",copy(str2,str1));
   //printf("%s",str2);
   return 0;
}
