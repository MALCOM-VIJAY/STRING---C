#include <stdio.h>
#include <math.h>
int len(char str[]){
    int  i=0;
    while(str[i]!='\0')
        i++;
    return i;
}
int main()
{
    char str1[10]="hellolhlh";
    printf("%d",len(str1));
    return 0;
}
